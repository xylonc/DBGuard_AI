# Image Catalog for DBGuardAI
# Each YAML file represents an approved PostgreSQL image profile.
# Images are referenced by immutable digest — mutable tags are NEVER used.
# Only APPROVED entries that haven't expired can be selected.
