# Server-only values. Never prefix secrets with VITE_.
UI_PORT=8443
DBGUARD_API_URL=http://127.0.0.1:8011
DBGUARD_DEMO_URL=http://127.0.0.1:8010
HERMES_API_URL=http://127.0.0.1:8642
HERMES_WORKFLOW_BACKEND=demo
# HERMES_API_SERVER_KEY must match the running HERMES API server.
# Load it from an existing private environment file with DBGUARD_ENV_FILE.
