# Reporting status: deferred

The reporting module is retained as implemented library code, but its current
models are tied to the deferred assessment workflow. It is excluded from the
runnable proposal-phase deployment until proposal review-package contracts are
defined.
